## Skill depth and comfort levels

Marvin's strongest skills are Python and the core machine learning stack
(Scikit-learn, PyTorch, Pandas, NumPy) — he understands the typical ML
project workflow well, end to end. He's used TensorFlow less, though
doesn't expect it'd be hard to pick up.

His R is a bit rusty (hasn't used it much since undergrad). SQL is
self-taught, and he brushes up on it regularly, though query optimization
is an area he'd like to sharpen. Deployment, cloud services, and
Databricks/Spark are skills he's comfortable with but hasn't used
extensively at real industry scale yet. Same with Excel — comfortable, not
expert, but not expecting it to be hard to level up.

## Favorite stack

Pandas and Spark for data ingestion and feature engineering, PyTorch and
Scikit-learn for modeling, and Docker to containerize a prototype before
deploying it to a cloud service.
